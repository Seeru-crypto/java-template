spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:8881/DEFAULT_DB
logging:
  level:
    root: info
corsAllowedOrigin: http://localhost:3000